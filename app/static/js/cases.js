// Базовая инициализация для страницы "Кейсы"
document.addEventListener('DOMContentLoaded', function () {
    const casesSection = document.querySelector('.cases-list');
    if (!casesSection) {
        return;
    }

    // Здесь в будущем можно добавить фильтры/поиск/загрузку кейсов с бэкенда
    console.log('Страница "Кейсы" загружена');
});


