// Базовая инициализация для страницы "О компании"
document.addEventListener('DOMContentLoaded', function () {
    const aboutSection = document.querySelector('.about-main');
    if (!aboutSection) {
        return;
    }

    // Здесь в будущем можно добавить анимации или интерактивные блоки
    console.log('Страница "О компании" загружена');
});


